node_modules
src-tauri/notebook-runtime/*
!src-tauri/notebook-runtime/README.md
# Downloaded macOS ONNX Runtime dylib used only during release packaging.
src-tauri/onnxruntime/
notebook/**/__pycache__/
test-results/
playwright-report/
scripts/__pycache__/
.freebuff
graphify-out

# Tauri updater signing material must never enter the repository.
myapp.key
myapp.key.pub
.tauri/

# Vite dev cache (pre-bundled deps) — auto-regenerated on `vite dev`
.vite/

# Native ingestion build and downloaded runtime payloads.
src-tauri/src/ingest/target/
src-tauri/ingest-runtime/
